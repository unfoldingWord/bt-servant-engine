"""Intent handlers package.

Each module encapsulates a single intent's logic. brain.py delegates to these
handlers via thin wrappers to keep the public surface stable while keeping
modules small and cohesive.
"""

